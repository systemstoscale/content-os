node_modules/
.wrangler/
.dev.vars
*.log
.DS_Store
dist/

# Python bytecode (from local py_compile checks of the vendored pipeline)
__pycache__/
*.pyc

# Build artifacts (regenerated by `bun run build`)
.next/
out/
ui/.next/
ui/out/

# Public-repo sync clone (created by scripts/sync-to-public.sh)
build/

# Runtime outputs + audit logs (not source)
outputs/
ui/outputs/
container/outputs/
*.jsonl

# Operator DB schema dump — DDL source of truth is migrations/
schema.sql

# Secrets — never commit (infra keys live in KV/SECRETS bindings)
.env
.env.*
credentials.json
token.json
